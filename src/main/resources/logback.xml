<!-- https://www.playframework.com/documentation/latest/SettingsLogger -->
<configuration>
    <include optional="true" resource="logback_local.xml"/>
<!--    <conversionRule conversionWord="coloredLevel" converterClass="play.api.libs.logback.ColoredLevel"/>-->

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${application.home:-.}/logs/application.log</file>
        <encoder>
            <pattern>%date [%level] from %logger in %thread - %message%n%xException</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
<!--            <pattern>%coloredLevel [%.13thread] %logger{15} - %message%n%xException{10}</pattern>-->
<!--            <pattern>%date [%level] from %logger in %thread - %message%n%xException</pattern>-->
            <pattern>%d{HH:mm:ss}[%level] %thread - %mdc{akkaSource} %message%n%xException</pattern>

        </encoder>
    </appender>

    <appender name="ASYNCFILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
    </appender>

    <appender name="ASYNCSTDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="STDOUT"/>
    </appender>

    <logger name="application" level="DEBUG"/>


    <!--  server related logs-->
<!--    <logger name="akka.actor" level="WARN"/>-->

    <root level="${root.level:-INFO}">
        <appender-ref ref="ASYNCFILE"/>
        <appender-ref ref="ASYNCSTDOUT"/>
    </root>

</configuration>
